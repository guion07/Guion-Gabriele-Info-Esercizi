public class SommaDispari {
    public static void main(String[] args) {

        // si scriva un frammento di codice che computi la somma dei primi n numeri interi positivi dispari.
        // Per esempio, se n fosse uguale a 9, il programma dovrebbe calcolare 1 + 3 + 5 + 7 + 9.
        
        int n = 9;
        int somma = 0;
        int numeroDispari = 1;

        for (int i = 1; i <= n; i++) {
            somma += numeroDispari;
            numeroDispari += 2;
        }

        System.out.println("La somma dei primi " + n + " numeri dispari è: " + somma);
    }
}